/*ESTE É UM PROJETO DE PROGRAMAÇÃO EM C QUE APRESENTA O MAIOR NÚMERO, O MENOR NÚMERO, A SOMA E A MÉDIA ENTRE NÚMEROS INTEIROS INTRODUZIDOS PELO UTILIZADOR. A COMPLICAÇÃO DO PROGRAMA RESIDE NO FACTO DE NÃO EXISTIR DE ANTEMÃO A QUANTIDADE DE NÚMEROS QUE O UTILIZADOR VAI INTRODUZIR, OU SEJA, O UTILIZADOR VAI COLOCANDO OS NÚMEROS ATÉ QUANDO CLICAR ANALISAR PARA OS RESULTADOS APARECEREM*/

#include <stdio.h>
#include <locale.h>

int quantidadeDeNumerosIntroduzidos(int quantidade){
    return (quantidade);
}

int introduzirNumeros(int valor){
    int numero;
    for(numero=0; numero<=valor; numero++){
        printf("conta as vezes");
    }
}
int main(){
}